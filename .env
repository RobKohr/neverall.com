# In the real world, this file would be called .env.example
# and .env would be .gitignored. The server admin would create a 
# copy of .env.example and change the values as needed.
# We are skipping this here to make it easier to just start up out of the box for the test.

PORT=4000
serverPort=4001
mongoUrl='mongodb://127.0.0.1:27017'
secret='NotAVerySecretSecretButStillGoodEnoughAsADefaultDontYouThink'
dbName='neverall'
REACT_APP_NOT_SECRET_CODE_API='/api/'
REACT_APP_API_BASE_URL=
